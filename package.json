{
  "name": "@qxp/growth",
  "version": "0.1.0",
  "description": "Agent-native growth experimentation control plane.",
  "bin": {
    "growth": "./dist/index.js",
    "growth-mcp": "./dist/mcp.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsx src/index.ts",
    "start": "node dist/index.js",
    "prepack": "npm run build",
    "dogfood:local": "npm run build && node outerloop/dogfood-local.mjs",
    "test": "npm run build && node --test test/*.test.js"
  },
  "files": [
    "dist/",
    "README.md"
  ],
  "publishConfig": {
    "access": "public"
  },
  "dependencies": {
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1",
    "commander": "^12.1.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  },
  "type": "module",
  "engines": {
    "node": ">=18"
  }
}
